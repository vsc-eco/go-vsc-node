# Use with: source .env.remote-devnet (after copying this to .env.remote-devnet)
# Point 3-node stack at Hive devnet on SSH server. Use the server IP so Docker can resolve it.
HIVE_API=http://157.180.53.238:8090
